[project]
name = "aura-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.111",
  "uvicorn>=0.30",
  "pydantic>=2.7",
  "httpx>=0.27",
  "playwright>=1.45",
  "pytest>=8.2"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
